export interface Product {
  sku: string;
  name: string;
  description: string;
  unitPrice: number;
  imageUrl: number;
  active: boolean;
  unitsInStock: number;
  dataCreated: Date;
  lastUpdate: Date;
}
